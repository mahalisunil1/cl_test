{
  "name": "components-layer",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create:component": "node scripts/create-component.mjs",
    "create:template": "node scripts/create-template.mjs",
    "dev:docs": "pnpm --filter docs dev",
    "dev:packages": "pnpm -r --filter @sunil/* build -- --watch",
    "build:packages": "pnpm -r --filter @sunil/* build",
    "typecheck:packages": "pnpm -r --filter @sunil/* exec tsc --noEmit",
    "validate:registry": "node scripts/validate-registry.mjs",
    "check:changeset": "node scripts/check-changeset.mjs",
    "build:registry": "node scripts/build-registry.mjs",
    "release:check": "pnpm build:packages && pnpm typecheck:packages && pnpm validate:registry",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "lint": "pnpm --filter docs lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "~5.9.3"
  }
}

